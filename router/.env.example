# Slack App 서명 시크릿 (api.slack.com → Basic Information → Signing Secret)
SLACK_SIGNING_SECRET=

# Expense Agent HTTP 엔드포인트 (동일 VM이면 localhost)
EXPENSE_AGENT_URL=http://localhost:3000/slack/events

# 기존 서비스(simpson/calendar) HTTP 엔드포인트
SIMPSON_SERVICE_URL=

# 라우터 포트
PORT=3001
LOG_LEVEL=INFO
